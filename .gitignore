*.xlsx
__pycache__/
.env
toprate_selections.csv
GitHub token.txt
api_dump.txt
*.log
__pycache__/
*.pyc

# Local copies / backups
*- Copy.csv
*- Copy.py
copy
git
main

# Experimental / one-off scripts
backfill_signals.py
debug_api_fields.py
fix_gitignore.py
fix_workflow.py
toprate_interactive.htmlpf_api.key 
pf_api2.txt 
toprate_cookies.txt
pf_api.key
wpr_form_history.csv
supabase_key.txt
supabase_key.txt
toprate_credentials.txt
catboost_info/

# Frontend build tooling (frontend/) - node_modules and the raw Vite build
# output are not tracked; only the built toprate_live.html at repo root is
# (same artifact-tracking convention as everything else in this repo).
frontend/node_modules/
frontend/dist/

# tab_results_poller.py runs locally on an AU machine only (see
# TAB_API_NOTES.md); its terminal-race cache and raw JSON archive are
# per-machine scratch state, not shared repo data.
tab_poller_terminal_races.json
tab_raw/
